# cc-test Checkpoints

填写约束：
- `结果` 列必须填写 `✅`、`❌`、`⚠️` 或 `N/A`
- 不要在 `检查项` 列使用 `[ ]` / `[x]` 充当结果

## 开始前检查

| 检查项 | 结果 |
|--------|------|
| `spec.md` 已存在 | ✅ / ❌ / ⚠️ / N/A |
| `spec.status` 为 `apply` 或 `review` | ✅ / ❌ / ⚠️ / N/A |
| 已读取本次 change 声明的最低验证等级 | ✅ / ❌ / ⚠️ / N/A |
| 已明确本次测试层级选择与原因 | ✅ / ❌ / ⚠️ / N/A |

## 执行中检查

| 检查项 | 结果 |
|--------|------|
| 已优先采用 Red / Green；若未采用，已说明退化原因 | ✅ / ❌ / ⚠️ / N/A |
| 已生成或更新 `test-spec.md` | ✅ / ❌ / ⚠️ / N/A |
| 已逐个运行测试并展示验证证据 | ✅ / ❌ / ⚠️ / N/A |
| 已说明为什么当前测试层级足以覆盖本次风险 | ✅ / ❌ / ⚠️ / N/A |

## 完成后检查

| 检查项 | 结果 |
|--------|------|
| `go test -cover` 输出已展示 | ✅ / ❌ / ⚠️ / N/A |
| 验证证据已覆盖声明的最低验证等级 | ✅ / ❌ / ⚠️ / N/A |
| 若未执行严格 Red→Green，`test-spec.md` 已说明原因 | ✅ / ❌ / ⚠️ / N/A |
