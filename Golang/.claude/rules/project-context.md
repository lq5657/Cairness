---
alwaysApply: true
---
### 工程上下文

首次使用时执行 /init 让 AI 分析工程并填充本文件。

补充边界：
- `/init` 只负责识别项目事实并回写本文件
- `/init` 不负责创建 `changes/examples/`、`changes/templates/` 或示例 change
- 若要做存量项目体检，使用 `/inspect-codebase`，不要伪装成 `/init` 或 `/review`

#### 使用说明

- 本文件用于记录项目事实，不用于强行套用预设架构
- 已有项目：优先记录“实际观察到”的目录、分层、依赖和团队约定
- 新项目：允许填写“初始化建议”，但必须标明哪些内容尚未落地

#### 1. 项目类型

* 模式: 已有项目 / 新项目
* 当前阶段: 调研中 / 初始化完成 / 已接入规范

#### 2. 应用概况

* 应用名: （待填充）
* 简介: （一句话描述）
* 技术栈: （根据项目实际填写，不预设 Gin/GORM）
* 构建工具: Go Modules (`go.mod`) / 其他
* 运行形态: HTTP API / gRPC / Worker / CLI / 混合

#### 3. 目录结构与模块职责

执行 `tree -d -L 3` 后填充，并说明关键目录职责。

#### 4. 实际分层与调用关系

按项目实际填写。示例：

API/Transport
↓
Application/Service
↓
Domain/Manager
↓
Repository/DAO

如果项目没有明显分层，明确写“当前未分层”，不要虚构结构。

#### 5. 关键依赖

| 依赖/中间件 | 用途 | 备注 |
|-------------|------|------|
| （根据项目填写） | | |

#### 6. 代码约定与团队规范

| 主题 | 当前约定 | 证据 |
|------|----------|------|
| 日志 | | |
| 错误处理 | | |
| 配置管理 | | |
| 测试策略 | | |
| 并发模式 | | |

#### 6.1 日志方案

| 项目 | 当前约定 | 备注 |
|------|----------|------|
| 默认日志库 | `log/slog` / 其他 | |
| 输出目标 | 日志文件 / stdout + collector / 混合 | |
| 日志路径 | 由使用者决定 | 若落盘必须明确路径规则 |
| 日志格式 | 时间、等级、消息、文件名:行号、函数/方法名 | 时间默认到微秒 |
| 切分策略 | 按天 / 按大小 / 平台托管 | |
| 保留策略 | 7天 / 30天 / 平台托管 | |
| Trace/Request ID | 是否要求 | |

#### 6.2 配置管理

| 项目 | 当前约定 | 备注 |
|------|----------|------|
| 配置来源 | 环境变量 / 配置文件 / Secret 管理 / 平台注入 / 混合 | |
| 读取入口 | 启动期 / 依赖注入层 / 分散读取 | |
| 默认值策略 | 显式默认值 / 启动失败 / 混合 | |
| 必填校验 | 启动期校验 / 懒校验 / 无 | |
| 敏感配置处理 | Secret / 脱敏 / 禁止落日志 | |
| 环境差异 | dev/test/staging/prod 的主要差异 | |
| Feature Flag | 是否使用及管理方式 | |

#### 6.3 可观测性

| 项目 | 当前约定 | 备注 |
|------|----------|------|
| 关键日志字段 | request_id / trace_id / user_id / task_id / 其他 | |
| Metrics | Prometheus / 平台监控 / 无 | |
| Alerting | 平台告警 / 自定义阈值 / 无 | |
| Tracing | OpenTelemetry / 自研 / 无 | |
| 异步任务观测 | 重试、死信、补偿的记录方式 | |
| 发布观察窗口 | 发布后默认观察哪些指标、多长时间 | |

#### 6.4 测试策略

| 项目 | 当前约定 | 备注 |
|------|----------|------|
| 单测主框架 | testing / testify / gomock / 其他 | |
| Repo 测试方式 | 真实库 / testcontainer / mock / 无 | |
| Transport 测试方式 | HTTP test / gRPC test / 无 | |
| 链路回归方式 | 本地回归 / 脚本 / 平台用例 / 无 | |
| 集成验证方式 | sandbox / staging / mock 平台 / 无 | |
| Bugfix 回归要求 | 是否要求至少一条回归证据 | |

#### 7. 领域特性与高风险点

| 类型 | 描述 | 备注 |
|------|------|------|
| 资金 | | |
| 状态流转 | | |
| 权限 | | |
| 外部依赖 | | |

#### 8. 初始化建议（仅新项目填写）

| 主题 | 建议 | 状态 |
|------|------|------|
| 项目骨架 | | 待确认/已确认 |
| 基础依赖 | | 待确认/已确认 |
| 分层方案 | | 待确认/已确认 |
