### 需求名称

```
change_id: kebab-case-id
status: propose | apply | review | done
depends_on: []            # 可选；依赖的 change-id 列表
parallel_safe: true | false
created: YYYY-MM-DD
updated: YYYY-MM-DD
complexity: 🟢简单 | 🟡中等 | 🔴复杂
```

#### 文档约束

- 文件位置：`changes/<change-id>/spec.md`
- `status` 必须与实际阶段一致：
  - `propose`：提案已确认，尚未开始编码
  - `apply`：编码进行中，允许 task 边界内的受控偏差
  - `review`：编码完成，要求 spec 与代码一致
  - `done`：归档完成
- `depends_on` 用于声明跨变更依赖；无依赖时写 `[]`
- `parallel_safe` 表示该变更是否允许与其他变更并行推进
- 实现中如果发现 spec 不足或错误，必须先更新本文件，再继续编码
- 若命令执行失败或中断，必须在 §12 记录 `blocked` / `partial` / `aborted`

#### 1. 背景与目标

为什么做 + 做完后的效果（可验证的结果描述）

#### 2. 代码现状（Research Findings）

每个结论必须有代码出处（文件路径 + 结构体名/函数名）

##### 2.1 相关入口与链路

##### 2.2 现有实现

##### 2.3 发现与风险

#### 3. 功能点

* [ ] 功能 1：（输入→处理→输出）

#### 4. 业务规则

⚠️ 涉及以下任一场景必须在此标注 `⚠️ REQ-HUMAN-REVIEW` 并说明影响面：
- 资金变更：余额、扣款、退款、冻结、解冻、授信、手续费
- 状态流转：影响业务结果、资金结果、权限结果的状态迁移
- 权限变更：角色、资源授权、可执行操作范围变更

#### 5. 数据变更

| 操作 | 表名 | 字段/索引 | 说明 |

#### 6. 接口变更

| 操作 | 接口 | 方法 | 变更内容 |

#### 7. 影响范围

#### 8. 风险与关注点

| 类型 | 描述 | 处理方式 |
|------|------|----------|
| ⚠️ 资金 | （如涉及） | 需人工审查 |
| ⚠️ 状态 | （如涉及） | 检查状态机合法性 |
| ⚠️ 权限 | （如涉及） | 显式校验 |

#### 9. 测试策略

* **测试范围**：
* **覆盖率目标**：P0≥80%, P1≥60%
* **独立 Test Spec**：是/否

#### 10. 待澄清

* [ ] 问题 1：（全部解决后才能进入 /apply）

#### 11. 技术决策

| 决策 | 选择 | 放弃的方案 | 原因 |

#### 12. 执行日志

| Task | 状态 | 实际改动文件 | 备注 |
|------|------|-------------|------|

状态建议值：
- `待做`
- `进行中`
- `blocked`
- `partial`
- `aborted`
- `已完成`

#### 13. 审查结论

* **Stage 1 / Spec Compliance**：
* **Stage 2 / Code Quality**：
* **总体结论**：可进入 `/fix` / 可归档

#### 14. 确认记录（HARD-GATE）

* **确认时间**：
* **确认人**：
