### 需求名称

```
change_id: kebab-case-id
status: propose | apply | review | done
depends_on: []            # 可选；依赖的 change-id 列表
parallel_safe: true | false
branch: feat/<change-id>
created: YYYY-MM-DD
updated: YYYY-MM-DD
complexity: 🟢简单 | 🟡中等 | 🔴复杂
```

#### 文档约束

- 文件位置：`changes/<change-id>/spec.md`
- `status` 必须与实际阶段一致：
  - `propose`：提案已确认，尚未开始编码
  - `apply`：编码进行中，允许 task 边界内的受控偏差
  - `review`：编码完成，要求 spec 与代码一致
  - `done`：归档完成
- `depends_on` 用于声明跨变更依赖；无依赖时写 `[]`
- `parallel_safe` 表示该变更是否允许与其他变更并行推进
- `branch` 用于声明当前 change 绑定的工作分支；若偏离推荐命名，必须在 `log.md` 记录原因
- 实现中如果发现 spec 不足或错误，必须先更新本文件，再继续编码
- 若命令执行失败或中断，必须在“执行日志”章节记录 `blocked` / `partial` / `aborted`

#### 1. 背景与目标

为什么做 + 做完后的效果（可验证的结果描述）

#### 1.1 本次不做

明确列出不在本次 change 范围内的功能、不会顺手处理的历史问题、暂不处理的优化项。

#### 2. 代码现状（Research Findings）

每个结论必须有代码出处（文件路径 + 结构体名/函数名）

若是绿地项目或本地缺少可参考实现，可写明：
- 当前仓库暂无既有实现可作为直接依据
- 已补充受控外部 Research，来源应优先为成熟开源项目、事实标准、主流设计模式或官方文档
- 外部 Research 只用于提炼候选方案、关键权衡和适配条件，不替代本项目的正式提案

##### 2.1 相关入口与链路

##### 2.2 现有实现

##### 2.3 发现与风险

#### 3. 功能点

* [ ] 功能 1：（输入→处理→输出）

#### 4. 业务规则

⚠️ 涉及以下任一场景必须在此标注 `⚠️ REQ-HUMAN-REVIEW` 并说明影响面：
- 资金变更：余额、扣款、退款、冻结、解冻、授信、手续费
- 状态流转：影响业务结果、资金结果、权限结果的状态迁移
- 权限变更：角色、资源授权、可执行操作范围变更

#### 5. 数据变更

若本次不涉及，直接写 `无` 即可；若只需表达单一结论，可保留字段说明，不必强行补满整张表。

* **是否涉及 migration**：是/否
* **migration / 脚本路径**：
* **变更类型**：expand / migrate / contract / 无
* **兼容窗口**：
* **回滚路径**：
* **数据回填方案**：
* **幂等性与失败恢复**：

| 操作 | 表名 | 字段/索引 | 说明 | 风险 |

#### 6. 接口变更

若本次不涉及，直接写 `无` 即可；若只新增或调整单一接口，可只写核心字段与一行表格说明。

* **是否涉及对外契约变更**：是/否
* **兼容性分类**：compatible_addition / compatible_adjustment / breaking_change / 无
* **客户端/消费者影响**：
* **迁移路径**：
* **回滚影响**：

| 操作 | 接口 | 方法 | 变更内容 | 兼容性 |

#### 7. 影响范围

#### 7.1 配置变更

若本次不涉及，直接写 `无` 即可；不要为了凑模板强行展开并不存在的配置治理细节。

* **是否涉及配置项或环境变量变更**：是/否
* **配置来源**：环境变量 / 配置文件 / Secret 管理 / 平台注入 / 无
* **新增/变更配置项**：
* **默认值与是否安全**：
* **是否必填**：
* **生效范围**：
* **环境差异**：
* **回滚影响**：

#### 8. 风险与关注点

| 类型 | 描述 | 处理方式 |
|------|------|----------|
| ⚠️ 资金 | （如涉及） | 需人工审查 |
| ⚠️ 状态 | （如涉及） | 检查状态机合法性 |
| ⚠️ 权限 | （如涉及） | 显式校验 |
| ⚠️ 数据库 | （如涉及 migration / 回填 / schema 收缩） | 说明兼容窗口与回滚路径 |
| ⚠️ 接口契约 | （如涉及 HTTP/gRPC/MQ breaking change） | 说明客户端影响、迁移路径和回滚影响 |
| ⚠️ 配置 | （如涉及默认值、必填性、环境差异、开关） | 说明配置来源、默认值安全性和回滚影响 |

#### 8.1 日志与可观测性

只记录本次 change 真正新增或必须确认的内容；若没有新增日志点或观察要求，可写 `无新增要求`。

* **是否新增运行时日志点**：
* **涉及哪些入口/任务**：
* **使用的 logger**：默认 `log/slog`
* **关键字段**：request_id / trace_id / task_id / 业务主键等
* **日志落点**：默认建议日志文件；具体路径由使用者决定
* **日志格式**：时间（微秒）、等级、消息、文件名:行号、函数/方法名
* **Metrics / 告警观察项**：
* **发布后观察窗口**：
* **替代方案**：若不落盘，说明 stdout/stderr + 平台采集方案

#### 9. 测试策略

* **测试范围**：
* **最低验证等级**：L1 / L2 / L3 / L4 / L5
* **验证证据要求**：
* **若无法达到目标等级的替代方案**：

#### 9.1 需求-验证映射

这里是约束，不是参考建议。每条高风险需求或风险点都必须映射到至少一种可展示的验证证据；若本次很简单，可只保留核心几行，不必机械铺满。

| 需求项 / 风险点 | 最低验证等级 | 证据类型 | 建议验证动作 | 对应 Task |
|------|----------------|----------|--------------|-----------|
| | L1 / L2 / L3 / L4 / L5 | build / package / chain / integration / manual / release-safety | | |

#### 9.2 发布与回滚

* **发布方式**：直接发布 / 灰度 / 分批 / 开关控制 / 前滚 / 其他
* **Feature Flag / Kill Switch**：
* **回滚路径**：代码回滚 / 配置回滚 / 数据回滚 / 开关回拨 / 补偿方案
* **若无法直接回滚的原因**：
* **发布后观察窗口**：
* **失败触发条件**：

#### 10. 待澄清

记录仍影响设计确认、范围冻结或 task 拆分的未决问题。全部解决后才能进入 `/apply`。

* [ ] 问题 1：

#### 11. 方案比较

| 方案 | 是否采用 | 适用前提 | 采用 / 放弃原因 |

#### 12. 技术决策

| 决策 | 选择 | 放弃的方案 | 原因 |

#### 13. 执行日志

| Task | 状态 | 实际改动文件 | 备注 |
|------|------|-------------|------|

状态建议值：
- `todo`
- `in_progress`
- `blocked`
- `partial`
- `aborted`
- `done`

#### 14. 审查结论

* **Stage 1 / Spec Compliance**：
* **Stage 2 / Code Quality**：
* **总体结论**：可进入 `/fix` / 可归档

#### 15. 确认记录（HARD-GATE）

* **确认时间**：
* **确认人**：
