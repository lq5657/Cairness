### 变更日志 — 新增用户创建接口

记录决策、踩坑和知识发现。知识飞轮的输入。

#### 时间线

| 时间 | 阶段 | 事件 | 备注 |
|------|------|------|------|
| 2026-04-11 14:00 | propose | 完成代码现状调研和需求收敛 | 明确以 email 作为幂等键 |
| 2026-04-11 14:05 | propose | 检查与并行中的“用户详情展示优化”需求是否冲突 | 仅共享 `UserHandler` 入口，不改同一创建链路，记录为可并行 |
| 2026-04-11 14:20 | apply | 完成 Service/Repo 创建链路 | 先实现业务核心 |
| 2026-04-11 14:40 | test | 补 Service 核心测试 | 覆盖成功和重复 email |
| 2026-04-11 15:00 | review | 两阶段审查通过 | 无阻塞问题 |

#### 技术决策

| 决策 | 选择 | 放弃的方案 | 原因 |
|------|------|------------|------|
| 幂等校验层次 | 放在 Service | 放在 Handler | 防止绕过入口 |
| 错误表达 | 业务错误常量 + 包装 | 直接返回 DB 唯一索引错误 | 对调用方更稳定 |
| 并发推进策略 | 允许与查询类需求并行 | 所有用户域需求强制串行 | 当前改动集中在创建链路，冲突面可识别且可规避 |

#### Git / 验证记录

| 时间 | 分支/提交动作 | 影响范围 | 验证等级 | 备注 |
|------|---------------|----------|----------|------|
| 2026-04-11 14:15 | `feat/user-create-api` 提交 Task 1 | service/repo/model | L2 | 主体实现完成 |
| 2026-04-11 14:45 | `feat/user-create-api` 提交 Task 2 | handler/test | L3 | 补链路回归说明与核心测试 |

#### 配置记录

| 时间 | 配置项 | 变更类型 | 默认值/必填性 | 环境差异/回滚说明 |
|------|--------|----------|---------------|-------------------|
| 无 | 无 | 无 | 无 | 无 |

#### 发布 / 回滚记录

| 时间 | 发布动作 | 开关/灰度策略 | 回滚或补偿动作 | 观察结果 |
|------|----------|---------------|----------------|----------|
| 2026-04-11 15:05 | 直接发布创建接口 | 无 | 如失败直接回滚代码 | 观察 30 分钟，成功率稳定 |

#### 踩坑记录

| 问题 | 原因 | 解决方案 | 已沉淀？ |
|------|------|----------|----------|
| 重复 email 逻辑容易只放在 Handler | 入口层最先接收参数，容易误把校验放全 | 明确“幂等和业务规则在 Service” | 否 |

#### 知识发现

**判断标准（满足任一则记录）：**
- Go 最佳实践与团队规范的差异
- 排查问题时发现的有价值的根因
- 技术决策及其理由
- 第三方库的特定坑点

**格式：** `/** 关键词 */` — 描述 → 出处

| 关键词 | 描述 | 出处 | 已沉淀？ |
|--------|------|------|----------|
| `/** service-idempotency */` | 写接口的幂等规则应落在 Service，而不是只放在 Handler | `changes/examples/user-create-api/spec.md` §11 | 否 |

#### Spec-Code 偏差记录

| 偏差点 | Spec 预期 | 实际情况 | 处理方式 |
|--------|-----------|----------|----------|
| 无 | - | - | - |

#### 并发记录

| 对象 | 类型 | 结论 | 记录位置 |
|------|------|------|----------|
| `user-profile-query-polish` | 调用链邻近但不同写路径 | 可并行；若改动 `user_handler.go` 同一区块则转人工确认 | `spec.md` §11, `tasks.md` Task 2 |

#### 代码质量备忘

- Handler 只做参数校验和错误映射，不下沉业务规则
- 业务错误必须可被测试稳定断言
- 创建接口新增日志字段要与现有 `request_id` 约定保持一致
