---
alwaysApply: false
description: "当涉及业务领域特定逻辑时应用本规则"
---
### 业务领域约束

#### 1. 通用领域规则 (结合 Go 数据类型)

* 所有金额使用 `int64` 类型，单位为分。
* 时间字段统一使用 `time.Time` 类型。
* 外部接口调用必须设置超时（默认 3s，使用 `context.WithTimeout`）并做降级处理。
* 状态变更必须通过状态机，禁止直接修改状态字段 。

#### 2. 项目特定规则

（待实践中补充）
