# cc-review Checkpoints

## 开始前检查

| 检查项 | 结果 |
|--------|------|
| [ ] `spec.md` 已读 | |
| [ ] 代码已存在 | |
| [ ] `spec.status` 为 `review` | |
| [ ] 已确认本次是基于已有 change 的实现审查，不是 `cc-inspect-codebase` | |

## 阶段一检查

| 检查项 | 结果 | 备注 |
|--------|------|------|
| [ ] 缺失实现：spec 要求了但代码没做 | PASS / FAIL | |
| [ ] 多余实现：spec 没要求但代码多做了 | PASS / FAIL | |
| [ ] 理解偏差：做了但方向错误 | PASS / FAIL | |
| [ ] 业务规则落地：`spec.md` 的业务规则章节已体现 | PASS / FAIL | |
| [ ] 对外契约准确性：数据变更/接口变更声明与实现一致 | PASS / FAIL | |

## 阶段二检查

| 检查项 | 结果 |
|--------|------|
| [ ] 已检查 Critical 风险：安全、资金逻辑、并发安全、数据丢失 | |
| [ ] 已检查 Important 风险：错误处理、参数校验、兼容性、配置、可观测性、发布回滚 | |
| [ ] 已检查 Minor 风险：Go doc、注释、import、依赖使用 | |

## 完成后检查

| 检查项 | 结果 |
|--------|------|
| [ ] 已输出 `changes/<change-id>/review.md` | |
| [ ] 已将 `spec.md` 的“审查结论”章节同步为当前结论 | |
| [ ] 若 Stage 1 未完成，`stage2_status` 已记为 `skipped` | |
| [ ] 若中断，已记录未完成原因，未重置已有结论 | |
