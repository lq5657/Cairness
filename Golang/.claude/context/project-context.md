---
alwaysApply: true
---
### 工程上下文

首次使用时执行 `cc-init` 让 AI 建立“基础事实层”并填充本文件。
当需要更完整的项目事实画像时，再执行 `cc-enrich-context` 补充“补充事实层”。

补充边界：
- `cc-init` 只负责识别后续命令高频复用、且能低成本确认的基础事实
- `cc-enrich-context` 负责补充高解释成本但仍属于项目事实的内容
- `cc-init` 与 `cc-enrich-context` 都只回写本文件，不负责创建脚手架资产
- 若要做存量项目体检，使用 `cc-inspect-codebase`
- 若要输出面向人的系统讲解材料，使用 `cc-explain-system`

#### 使用说明

- 本文件用于记录项目事实与事实边界，不用于强行套用预设架构
- 已有项目：优先记录“实际观察到”的目录、入口、依赖和约定
- 新项目：只记录当前已知事实与待确认事项，不在本文件中承载设计建议
- 所有无法低成本确认的内容统一写入“待确认事项”，不要把推测写成事实

#### 结构说明

本文件分为两层：

1. 基础事实层：由 `cc-init` 建立，长期驻留上下文，用于后续命令快速定位
2. 补充事实层：由 `cc-enrich-context` 补充，用于后续命令获得更完整但仍以事实为主的系统认知

`cc-init` 目标不是把所有章节一次填满，而是让后续命令知道：
- 项目是什么
- 从哪里开始看
- 哪些内容已经确认
- 哪些内容仍待确认

## 一、基础事实层（`cc-init` 填充）

#### 1. 项目基本身份（必填）

* 模式: 已有项目 / 新项目
* 当前阶段: 调研中 / 初始化完成 / 已接入规范
* 应用名: （待填充）
* 简介: （一句话描述）
* 运行形态: HTTP API / gRPC / Worker / CLI / 混合

#### 2. 技术入口（必填）

* 构建工具: Go Modules (`go.mod`) / 其他
* 依赖入口: （如 `go.mod` 路径）
* 启动入口: （如 `cmd/api/main.go`、`cmd/worker/main.go`）
* 多入口形态: 单服务 / 多服务 / 多二进制 / 待确认

#### 3. 关键目录导航（必填）

只记录对后续命令有价值的目录及其已确认职责，不要求机械输出完整目录树。

| 目录 | 已确认职责 | 备注 |
|------|------------|------|
| （待填充） | | |

#### 4. 配置与测试入口（必填）

| 项目 | 已确认入口 | 备注 |
|------|------------|------|
| 配置入口 | （待填充） | |
| 测试入口 | （待填充） | |
| 日志入口 | （可待确认） | |

说明：
- `cc-init` 优先确认入口位置，不要求在此阶段展开配置治理、日志字段或测试策略细节
- 若入口无法低成本确认，写入“待确认事项”

#### 5. 关键依赖（必填）

只记录对定位系统有帮助的核心依赖或中间件，不追求完整清单。

| 依赖/中间件 | 用途 | 备注 |
|-------------|------|------|
| （根据项目填写） | | |

#### 6. 后续阅读导航（必填）

| 目标命令 | 建议优先阅读位置 | 备注 |
|----------|------------------|------|
| `cc-propose` | （待填充） | |
| `cc-inspect-codebase` | （待填充） | |
| `cc-explain-system` | （待填充） | |

## 二、补充事实层（`cc-enrich-context` 补充）

#### 7. 实际分层与调用关系（选填）

仅在可通过定向读取可靠确认时填写。
若证据不足，写“待确认”，不要基于目录命名强行推断分层。

按项目实际填写。示例：

API/Transport
↓
Application/Service
↓
Domain/Manager
↓
Repository/DAO

如果项目没有明显分层，明确写“当前未分层”，不要虚构结构。

#### 8. 代码约定与团队规范（选填）

| 主题 | 当前约定 | 证据 |
|------|----------|------|
| 日志 | | |
| 错误处理 | | |
| 配置管理 | | |
| 测试策略 | | |
| 并发模式 | | |

#### 9. 日志方案（选填）

| 项目 | 当前约定 | 备注 |
|------|----------|------|
| 默认日志库 | `log/slog` / 其他 | |
| 输出目标 | 日志文件 / stdout + collector / 混合 | |
| 日志路径 | 由使用者决定 | 若落盘必须明确路径规则 |
| 日志格式 | 时间、等级、消息、文件名:行号、函数/方法名 | 时间默认到微秒 |
| 切分策略 | 按天 / 按大小 / 平台托管 | |
| 保留策略 | 7天 / 30天 / 平台托管 | |
| Trace/Request ID | 是否要求 | |

#### 10. 配置管理（选填）

| 项目 | 当前约定 | 备注 |
|------|----------|------|
| 配置来源 | 环境变量 / 配置文件 / Secret 管理 / 平台注入 / 混合 | |
| 读取入口 | 启动期 / 依赖注入层 / 分散读取 | |
| 默认值策略 | 显式默认值 / 启动失败 / 混合 | |
| 必填校验 | 启动期校验 / 懒校验 / 无 | |
| 敏感配置处理 | Secret / 脱敏 / 禁止落日志 | |
| 环境差异 | dev/test/staging/prod 的主要差异 | |
| Feature Flag | 是否使用及管理方式 | |

#### 11. 可观测性（选填）

| 项目 | 当前约定 | 备注 |
|------|----------|------|
| 关键日志字段 | request_id / trace_id / user_id / task_id / 其他 | |
| Metrics | Prometheus / 平台监控 / 无 | |
| Alerting | 平台告警 / 自定义阈值 / 无 | |
| Tracing | OpenTelemetry / 自研 / 无 | |
| 异步任务观测 | 重试、死信、补偿的记录方式 | |
| 发布观察窗口 | 发布后默认观察哪些指标、多长时间 | |

#### 12. 测试策略（选填）

| 项目 | 当前约定 | 备注 |
|------|----------|------|
| 单测主框架 | testing / testify / gomock / 其他 | |
| Repo 测试方式 | 真实库 / testcontainer / mock / 无 | |
| Transport 测试方式 | HTTP test / gRPC test / 无 | |
| 链路回归方式 | 本地回归 / 脚本 / 平台用例 / 无 | |
| 集成验证方式 | sandbox / staging / mock 平台 / 无 | |
| Bugfix 回归要求 | 是否要求至少一条回归证据 | |

#### 13. 外部依赖与集成边界（选填）

| 依赖类型 | 描述 | 备注 |
|----------|------|------|
| 数据库 | | |
| 消息队列 | | |
| RPC / HTTP 下游 | | |
| 第三方平台 | | |

#### 14. 领域特性与高风险点（选填）

| 类型 | 描述 | 备注 |
|------|------|------|
| 资金 | | |
| 状态流转 | | |
| 权限 | | |
| 外部依赖 | | |

## 三、事实边界

#### 15. 已确认事实范围（必填）

说明本次已可靠确认到什么程度，便于后续命令判断是否需要继续补充上下文。

示例：
- 已确认单仓库、Go Modules、HTTP API 主入口和主要配置入口
- 已确认测试代码主要位于 `internal/` 与 `pkg/` 对应目录
- 尚未确认日志规范、可观测性与领域高风险点

#### 16. 待确认事项（必填）

记录本次 `cc-init` 或 `cc-enrich-context` 明确未确认、但后续可能影响审查或提案的内容。

示例：
- 实际日志初始化入口待确认
- 配置默认值策略待确认
- 测试分层约定待确认
- 是否存在统一 trace / metrics 方案待确认
