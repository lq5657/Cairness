# cc-test Checkpoints

填写约束：
- `结果` 列必须填写 `✅`、`❌`、`⚠️` 或 `N/A`
- 不要在 `检查项` 列使用 `[ ]` / `[x]` 充当结果

## 开始前检查

| 检查项 | 结果 |
|--------|------|
| `spec.md` 已存在 | ✅ / ❌ / ⚠️ / N/A |
| `spec.status` 为 `apply` 或 `review` | ✅ / ❌ / ⚠️ / N/A |
| 已读取本次 change 声明的最低验证等级 | ✅ / ❌ / ⚠️ / N/A |
| 已读取 `tasks.md` 中各 task 的测试要求 | ✅ / ❌ / ⚠️ / N/A |
| 已区分哪些验证已在 `cc-apply` 完成，哪些仍需在 `cc-test` 补齐 | ✅ / ❌ / ⚠️ / N/A |
| 已明确本次 `cc-test` 的目标是最低验证补齐还是更高等级补强 | ✅ / ❌ / ⚠️ / N/A |
| 已明确本次测试层级选择与原因 | ✅ / ❌ / ⚠️ / N/A |

## 执行中检查

| 检查项 | 结果 |
|--------|------|
| 已优先采用 Red / Green；若未采用，已说明退化原因 | ✅ / ❌ / ⚠️ / N/A |
| 已生成或更新 `test-spec.md` | ✅ / ❌ / ⚠️ / N/A |
| 当前测试设计已对齐 `spec.md` 的最低验证等级 | ✅ / ❌ / ⚠️ / N/A |
| 已逐个运行测试并展示验证证据 | ✅ / ❌ / ⚠️ / N/A |
| 已说明为什么当前测试层级足以覆盖本次风险 | ✅ / ❌ / ⚠️ / N/A |
| 若未执行更高层测试，已写明跳过原因和替代证据 | ✅ / ❌ / ⚠️ / N/A |

## 完成后检查

| 检查项 | 结果 |
|--------|------|
| `go test -cover` 输出已展示 | ✅ / ❌ / ⚠️ / N/A |
| 验证证据已覆盖声明的最低验证等级 | ✅ / ❌ / ⚠️ / N/A |
| 若未执行严格 Red→Green，`test-spec.md` 已说明原因 | ✅ / ❌ / ⚠️ / N/A |
| `test-spec.md` 已明确区分已完成验证与待补强验证 | ✅ / ❌ / ⚠️ / N/A |
| 验证证据已覆盖本次 change 的高风险点 | ✅ / ❌ / ⚠️ / N/A |
| 当前测试结论与 `tasks.md` 的测试要求不冲突 | ✅ / ❌ / ⚠️ / N/A |
